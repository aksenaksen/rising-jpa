spring:
  application:
    name: demo
  datasource:
    url: jdbc:mysql://localhost:3306/rising
    username: root
    password: 123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.example.elb: DEBUG
    org.hibernate.SQL: DEBUG